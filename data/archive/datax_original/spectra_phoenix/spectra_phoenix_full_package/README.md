# Spectra Phoenix Kafka Producer CLI

Interactive CLI tool to produce Spectra Phoenix events to Kafka.

## Installation

```bash
pip install .
```

## Usage

```bash
spectra-producer --bootstrap-server localhost:9092
```

Follow prompts to pick event types, edit fields, and send events.

Type `quit` to exit.
